export interface ErrorExplanation {
  /** 何が起きたか */
  title: string;
  /** 1〜2文の要約 */
  summary: string;
  /** なぜRustがこれを止めるのか */
  why: string;
  /** 対処法（箇条書き） */
  howToFix: string[];
  /** The Book 日本語版の該当章 */
  bookUrl?: string;
}

const BOOK_OWNERSHIP = "https://doc.rust-jp.rs/book-ja/ch04-00-understanding-ownership.html";
const BOOK_TYPES = "https://doc.rust-jp.rs/book-ja/ch03-02-data-types.html";
const BOOK_VARIABLES = "https://doc.rust-jp.rs/book-ja/ch03-01-variables-and-mutability.html";

/**
 * 初学者が高頻度で踏むrustcエラーコード → 日本語解説。
 * stderrから抽出したエラーコード(例: "E0382")をキーに引く。
 */
export const RUSTC_ERROR_JA: Record<string, ErrorExplanation> = {
  E0308: {
    title: "型が一致していません",
    summary: "期待されている型と、実際に渡された値の型が違います。",
    why:
      "Rustはすべての値の型をコンパイル時に確定させ、後から食い違いが出ないことを保証します。" +
      "そのため型が合わないコードはコンパイルが通りません。",
    howToFix: [
      "エラーメッセージの expected と found を見比べて、どちらを直すべきか判断する",
      "数値同士でも i32 と u32 のように型が違えば as で明示的に変換する必要がある",
      "関数の戻り値の型と、実際にreturnしている値の型を確認する",
    ],
    bookUrl: BOOK_TYPES,
  },
  E0382: {
    title: "ムーブ済みの値を使おうとしています",
    summary:
      "一度別の変数に「ムーブ(所有権の移動)」した値を、その後でまた使おうとしています。",
    why:
      "Stringなどヒープにデータを持つ型は、代入すると所有権そのものが移動し、" +
      "元の変数は無効になります。同じデータを2つの変数が同時に所有権を持つことを防ぐためのルールです。",
    howToFix: [
      "値を複製してよいなら .clone() を使う(コストはかかるが単純)",
      "所有権を渡さず借用したいだけなら & を付けて参照を渡す",
      "そもそも元の変数をもう使わないなら、後の行を削除する",
    ],
    bookUrl: BOOK_OWNERSHIP,
  },
  E0384: {
    title: "不変(immutable)な変数に再代入しようとしています",
    summary: "let で宣言した変数はデフォルトで不変(mutable)ではなく、値を変更できません。",
    why:
      "Rustは「変更しない」ことを既定にすることで、意図しない書き換えのバグを防ぎます。" +
      "変更したい場合は明示的にその意図を示す必要があります。",
    howToFix: [
      "変数を書き換えたいなら let mut で宣言し直す",
      "書き換えではなく新しい値を作りたいだけなら、同名でシャドーイング(let で再宣言)する",
    ],
    bookUrl: BOOK_VARIABLES,
  },
  E0425: {
    title: "名前が見つかりません",
    summary: "その名前の変数・関数がこのスコープには存在しません。",
    why: "Rustは未定義の名前の参照を許さず、コンパイル時にすべての名前解決を確定させます。",
    howToFix: [
      "スペルミスがないか確認する",
      "変数が定義されているブロック({ })の外から使おうとしていないか確認する(スコープ外)",
      "定義より前の行で使っていないか確認する",
    ],
  },
  E0433: {
    title: "モジュール・型のパスが見つかりません",
    summary: "use や :: で指定したパスの先にあるはずのものが見つかりません。",
    why: "Rustはモジュール構造を静的に解決するため、存在しないパスはコンパイル時に判明します。",
    howToFix: [
      "型名やモジュール名のスペルを確認する",
      "std:: の後に続くパスがそのモジュールに実在するか公式ドキュメントで確認する",
      "外部クレートを使う場合は Cargo.toml に依存関係を追加する",
    ],
  },
  E0499: {
    title: "可変な借用が同時に2つ存在しています",
    summary: "同じ値への &mut 参照を、同時に2つ以上作ろうとしています。",
    why:
      "可変な参照は「今この値を書き換えられるのはここだけ」という保証です。" +
      "同時に複数あるとどちらが値を書き換えるか予測できなくなるため、コンパイル時に禁止されています。",
    howToFix: [
      "片方の借用が終わってから、もう一方の借用を作る(スコープを分ける)",
      "本当に同時に必要か見直す。多くの場合は処理を順番に行えば解決する",
    ],
    bookUrl: BOOK_OWNERSHIP,
  },
  E0502: {
    title: "不変の借用と可変の借用が衝突しています",
    summary: "&T で借用している間に、同じ値を &mut T で借用しようとしています。",
    why:
      "「読み取り中に書き換えられる」ことを防ぐルールです。不変参照が使われている間は、" +
      "値が変わらないことが保証されます。",
    howToFix: [
      "不変参照を使い終える(その変数を最後に使う行より後に)から、可変参照を作る",
      "先に必要な値を読み取ってコピーしておき、その後で可変な操作をする",
    ],
    bookUrl: BOOK_OWNERSHIP,
  },
  E0507: {
    title: "参照の中身をムーブしようとしています",
    summary: "&T や &mut T の後ろにある値そのものを、参照経由でムーブしようとしています。",
    why: "参照は値を「借りている」だけで所有していません。借り物を持ち去ることはできません。",
    howToFix: [
      "値を複製してよいなら .clone() する",
      "参照のまま扱えるよう、値を取り出さずに操作する方法に書き換える",
    ],
    bookUrl: BOOK_OWNERSHIP,
  },
  E0596: {
    title: "不変の参照を可変であるかのように使おうとしています",
    summary: "&T (不変の参照)を通して値を書き換えようとしています。",
    why: "不変参照からは値を変更できません。変更してよいのは &mut T のときだけです。",
    howToFix: [
      "変更する必要があるなら、その参照を &mut T として作り直す",
      "参照元の変数自体が let mut で宣言されているか確認する",
    ],
    bookUrl: BOOK_OWNERSHIP,
  },
  E0277: {
    title: "必要なトレイトが実装されていません",
    summary: "この型には、この操作に必要な性質(トレイト)が備わっていません。",
    why:
      "Rustでは「この型はこの操作ができる」という保証をトレイトという仕組みで表します。" +
      "その保証がない型に対しては、対応する操作を呼び出せません。",
    howToFix: [
      "エラー中の `the trait ... is not implemented` を確認し、その型に必要なトレイトを実装するか、既に実装している型に変更する",
      "多くの場合 #[derive(...)] を型定義に追加することで解決する",
    ],
  },
  E0004: {
    title: "matchの分岐が網羅されていません",
    summary: "matchで、対象の型が取りうる値のパターンをすべて書ききれていません。",
    why:
      "Rustのmatchは「考慮漏れによるバグ」を防ぐため、すべての可能性を扱うことを" +
      "コンパイル時に強制します。実行時に思わぬ値が来て未対応、という事故が起きません。",
    howToFix: [
      "エラーメッセージの `not covered` に挙げられているパターン(例: None)の腕を追加する",
      "「それ以外すべて」を1つにまとめたいなら `_ => { ... }` というワイルドカードの腕を最後に追加する",
    ],
    bookUrl: "https://doc.rust-jp.rs/book-ja/ch06-02-match.html",
  },
  E0369: {
    title: "この型同士では演算子が使えません",
    summary: "+や*などの演算子を、それをサポートしていない型の組み合わせに使っています。",
    why:
      "Rustでは演算子も「その型が対応しているかどうか」がコンパイル時に決まります。" +
      "例えば `Result<i32, E>` はi32そのものではないため、直接 `*` で計算できません。",
    howToFix: [
      "`Result`や`Option`に包まれた値をそのまま計算に使おうとしていないか確認する",
      "`?` 演算子や `match`、`.unwrap()` などで中身の値を先に取り出してから演算する",
    ],
    bookUrl: "https://doc.rust-jp.rs/book-ja/ch09-02-recoverable-errors-with-result.html",
  },
  E0106: {
    title: "ライフタイム注釈が必要です",
    summary: "参照を含む型やシグネチャで、参照の有効期間(ライフタイム)を明示する必要があります。",
    why:
      "Rustは参照が指す先が有効な間しか使われないことをコンパイル時に保証します。" +
      "複数の参照の関係が自明でない場合は、その関係を明示する必要があります。",
    howToFix: [
      "まずは所有権を持つ型(String など)に変えられないか検討する(初学者向けの近道)",
      "参照のままにするなら 'a のようなライフタイム注釈を追加する",
    ],
  },
};

export function getErrorExplanation(code: string): ErrorExplanation | undefined {
  return RUSTC_ERROR_JA[code];
}
